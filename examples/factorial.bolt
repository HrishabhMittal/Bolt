package main

function factorial(number i32) (i32) {
    if number == 0 {
        return 1;
    } else {
        return number * factorial(number-1);
    }
}
function main() (i32) {
    z := findValue(13)+x*y;
    x := "Hello";
    num := divideBy5(10);
    return factorial(5);
}
