package main

x:=17;

function voidFunc() (void) {
    x := 5;
    if x > 0 {
        return;
    }
    return;
}

function findValue(target i32) (i32) {
    i := 0;
    while i != target {
        i = i + 1;
    }
    return i;
}
y:=findValue(13);
